// Placeholder widget - not used but kept for future enhancements
import 'package:flutter/material.dart';

class GlowingTabBar extends StatelessWidget {
  const GlowingTabBar({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
